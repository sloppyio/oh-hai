services:
  - docker

before_install:
  - docker build -t sloppyio/oh-hai:travis .

after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
    docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
    docker push USER/REPO;
    fi

script:
	- echo "huhu"

